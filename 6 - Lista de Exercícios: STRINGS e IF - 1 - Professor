#include <stdio.h>

int main(){
    char nome[100];
    int tamanho;
    printf("Digite o nome:");
    gets(nome);
    tamanho=strlen(nome);
    printf("capacidade da variavel: %d\n",sizeof(nome));
    printf("tamanho do conteudo: %d",tamanho);
}
